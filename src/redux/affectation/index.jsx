import { combineReducers } from 'redux'
import { reducer as newAffectation } from './newAffectation'

/*
 * render all redux reducers actions
 */

/*
 * render all redux reducers actions
 */
export default combineReducers({
    newAffectation,
})
